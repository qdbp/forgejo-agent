I couldn't land this automatically: the PR could not be merged fast-forward-only after rebasing.

- PR: {{pr_url}}
- branch: `{{branch}}` (base: `{{base_branch}}`)

Next action: please resolve the rebase conflicts on `{{branch}}`, push the updated branch, then retry by commenting:

{{retry_mention}}

Details:

```text
{{error}}
```

